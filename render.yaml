services:
  - type: web
    name: sistemaventa
    env: php
    buildCommand: |
      composer install --no-dev --optimize-autoloader
      php artisan config:cache
      php artisan route:cache
      php artisan view:cache
    startCommand: php artisan serve --host 0.0.0.0 --port $PORT
    plan: free

databases:
  - name: sistemaventa-db
    plan: free
